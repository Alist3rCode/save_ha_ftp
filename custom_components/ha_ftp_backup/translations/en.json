{
  "config": {
    "step": {
      "user": {
        "title": "Configure FTP Backup",
        "description": "{error_detail}",
        "data": {
          "ftp_host": "FTP Host",
          "ftp_port": "FTP Port",
          "ftp_user": "FTP Username",
          "ftp_password": "FTP Password",
          "ftp_path": "Remote Directory",
          "ftp_tls": "Use FTPS (TLS)",
          "backup_frequency_hours": "Backup Frequency (hours)",
          "max_backups": "Maximum Backups to Keep"
        }
      },
      "confirm": {
        "title": "FTP Connection Successful ✓",
        "description": "Connection established. Content visible on the server:\n\n```\n{listing}\n```\n\nClick Submit to finish the setup."
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to the FTP server.",
      "auth_failed": "Invalid credentials — username or password rejected by the server.",
      "host_not_found": "Host not found — check the FTP server address (DNS or IP).",
      "connection_refused": "Connection refused — check the port and that the FTP service is running.",
      "timeout": "Connection timed out — the server may be unreachable or the port is blocked by a firewall.",
      "ssl_error": "TLS/FTPS error — disable the FTPS option if your server does not support it."
    },
    "abort": {
      "already_configured": "This FTP server is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "FTP Backup Options",
        "data": {
          "ftp_path": "Remote Directory",
          "ftp_tls": "Use FTPS (TLS)",
          "backup_frequency_hours": "Backup Frequency (hours)",
          "max_backups": "Maximum Backups to Keep"
        }
      },
      "confirm": {
        "title": "FTP Directory Content",
        "description": "Content visible on the server:\n\n```\n{listing}\n```\n\nClick Submit to save the options."
      }
    }
  }
}
